# 🔥 Daily Python OOP Project #4 — Ready!
# (As always: beginner-friendly, practical, and uses OOP.)

# Here’s today’s project — clean, simple, and extremely useful:

# ✅ PROJECT: Personal Expense Tracker (OOP Version)

# A small system where a user can:
# Add expenses
# Categorize expenses
# View all expenses
# Filter expenses by category
# Calculate total spending
# Export spending summary to JSON

class Student:
    def __init__(self, name:str):
        self.name = name
        self.grades = {}
    
    def add_grade(self, subject, score):
        self.subject = subject 
        self.score = score

        self.grades["subject"] = self.subject
        self.grades["score"] = self.score

#TO BE CONTINUED!
    

